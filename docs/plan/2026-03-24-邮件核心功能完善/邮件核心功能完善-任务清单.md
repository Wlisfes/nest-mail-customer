# 邮件核心功能完善 - 任务清单

## 概览

| 项目     | 数值 |
| -------- | ---- |
| 总任务数 | 14   |
| 可并行   | 4    |
| 预计工时 | 4h   |

---

## US-1: 真实发送邮件

| 任务                        | 复杂度 | 并行 | 文件                                                     | 状态 |
| --------------------------- | ------ | ---- | -------------------------------------------------------- | ---- |
| Schema 增加 sendStatus 字段 | S      | ✅   | `server/modules/database/schema/tb_mail_message.ts`      | ⬜   |
| SMTP 发送逻辑               | M      | -    | `server/modules/mail-message/mail-message.service.ts`    | ⬜   |
| 发送失败存库标记            | S      | -    | `server/modules/mail-message/mail-message.service.ts`    | ⬜   |
| 重新发送接口                | S      | -    | `server/modules/mail-message/mail-message.controller.ts` | ⬜   |
| 前端发送失败提示与重试      | S      | -    | `web/views/manager/pages/manager-compose.vue`            | ⬜   |

### 详细任务

- [ ] [P][S] **T-001**: SchemaMailMessage 增加 `sendStatus` 字段（0=成功, 1=失败, 2=发送中） - `server/modules/database/schema/tb_mail_message.ts`

    - 验收标准: 实体字段定义完整，TypeORM 自动同步列

- [ ] [M] **T-002**: 使用 nodemailer 实现 SMTP 真实发送 (依赖 T-001) - `server/modules/mail-message/mail-message.service.ts`

    - 验收标准: 调用 `nodemailer.createTransport` 通过账号 SMTP 配置发送邮件，发送成功后存库 sendStatus=0

- [ ] [S] **T-003**: SMTP 失败时存库标记 sendStatus=1 (依赖 T-002) - `server/modules/mail-message/mail-message.service.ts`

    - 验收标准: 捕获 SMTP 异常，邮件仍存入数据库但 sendStatus=1，前端收到失败提示

- [ ] [S] **T-004**: 新增重新发送接口 POST /api/mail-message/resend (依赖 T-003) - `server/modules/mail-message/mail-message.controller.ts`

    - 验收标准: 传入 keyId 重新 SMTP 发送，成功后更新 sendStatus=0

- [ ] [S] **T-005**: 前端已发送列表显示发送状态，失败邮件支持重试按钮 - `web/views/manager/pages/manager-sent.vue`
    - 验收标准: 失败邮件行显示红色标记，点击重试调用 resend 接口

---

## US-2: 草稿箱数据展示与编辑回填

| 任务                         | 复杂度 | 并行 | 文件                                              | 状态 |
| ---------------------------- | ------ | ---- | ------------------------------------------------- | ---- |
| 草稿列表增加 userId 过滤     | S      | ✅   | `server/modules/mail-draft/mail-draft.service.ts` | ⬜   |
| 修复前端草稿列表数据解析     | S      | ✅   | `web/views/manager/pages/manager-drafts.vue`      | ⬜   |
| 草稿编辑回填：路由传参       | M      | -    | `web/views/manager/pages/manager-drafts.vue`      | ⬜   |
| Compose 页面接收草稿数据回填 | M      | -    | `web/views/manager/pages/manager-compose.vue`     | ⬜   |
| 草稿发送成功后自动删除       | S      | -    | `web/views/manager/pages/manager-compose.vue`     | ⬜   |

### 详细任务

- [ ] [P][S] **T-006**: 草稿列表查询增加 `userId` 过滤条件 - `server/modules/mail-draft/mail-draft.service.ts`

    - 验收标准: `httpFetchDrafts` 方法 WHERE 条件包含 `t.userId = :userId`

- [ ] [P][S] **T-007**: 修复前端草稿列表响应解析 - `web/views/manager/pages/manager-drafts.vue`

    - 验收标准: 草稿箱页面正确显示草稿列表数据

- [ ] [M] **T-008**: 草稿「编辑」跳转传递草稿 ID (依赖 T-007) - `web/views/manager/pages/manager-drafts.vue`

    - 验收标准: 点击编辑跳转 `/manager/compose?draftId=xxx`

- [ ] [M] **T-009**: Compose 页面根据 draftId 加载草稿数据回填 (依赖 T-008) - `web/views/manager/pages/manager-compose.vue`

    - 验收标准: 收件人、抄送、密送、主题、正文全部回填

- [ ] [S] **T-010**: 草稿邮件发送成功后自动删除草稿记录 (依赖 T-009) - `web/views/manager/pages/manager-compose.vue`
    - 验收标准: 发送成功后调用 `httpDeleteDraft(draftId)`

---

## US-3: 邮件详情查看

| 任务                        | 复杂度 | 并行 | 文件                                                             | 状态 |
| --------------------------- | ------ | ---- | ---------------------------------------------------------------- | ---- |
| IMAP 同步拉取邮件正文       | M      | ✅   | `server/modules/mail-account/mail-account.service.ts`            | ⬜   |
| 收件箱/已发送添加行点击跳转 | S      | -    | `web/views/manager/pages/manager-inbox.vue` + `manager-sent.vue` | ⬜   |
| 详情页区分收件/已发送展示   | M      | -    | `web/views/manager/pages/manager-mail-detail.vue`                | ⬜   |
| 端到端验证                  | S      | -    | 全链路                                                           | ⬜   |

### 详细任务

- [ ] [P][M] **T-011**: IMAP 同步时拉取邮件正文（htmlBody/textBody） - `server/modules/mail-account/mail-account.service.ts`

    - 验收标准: `syncFolder` 方法 fetch 时加入 `source: true`，解析并存储 htmlBody/textBody

- [ ] [S] **T-012**: 收件箱/已发送表格添加 rowProps 行点击跳转 - `web/views/manager/pages/manager-inbox.vue` + `manager-sent.vue`

    - 验收标准: 点击邮件行跳转 `/manager/mail/:id`

- [ ] [M] **T-013**: 邮件详情页根据 folder 区分展示 (依赖 T-012) - `web/views/manager/pages/manager-mail-detail.vue`

    - 验收标准: 收件邮件显示「发件人」，已发送邮件显示「收件人」

- [ ] [S] **T-014**: 端到端功能验证 - 浏览器测试
    - 验收标准: 发送邮件真实投递、草稿可编辑回填、邮件可查看详情

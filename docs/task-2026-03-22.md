# 根据页面 Mock 数据实现后端接口 - 任务清单

> 日期：2026-03-22

## 后端
- [x] 创建 Dashboard 模块 (module/service/controller)
- [x] 注册 DashboardModule 到 app.module.ts

## 前端 API 服务
- [x] 创建 web-dashboard.service.ts
- [x] 更新 web/api/index.ts 导出

## 前端页面对接
- [x] manager-stat-cards.vue → dashboard/stats
- [x] manager-chart-area.vue → dashboard/trend + distribution
- [x] manager-recent-mails.vue → mail-message/list
- [x] manager-inbox.vue → mail-message/list + 全部标记已读
- [x] manager-sent.vue → mail-message/list (Sent)
- [x] manager-drafts.vue → mail-draft/list + delete
- [x] manager-compose.vue → mail-account/list + send + save draft
- [x] manager-accounts.vue → mail-account CRUD
- [x] manager-blacklist.vue → mail-blacklist CRUD

## 验证
- [x] TypeScript 编译 0 错误
- [x] dev server 启动正常
- [x] 端口关闭

---

# 接口统一 POST 改造 - 任务清单

> 日期：2026-03-22

## 后端 Controllers
- [x] user.controller.ts — `GET /resolver` → `POST /resolver`
- [x] mail-account.controller.ts — `GET /list`, `POST /`, `PUT /:keyId`, `DELETE /:keyId` → 全部 POST
- [x] mail-message.controller.ts — `GET /list`, `PUT /seen/:keyId` → 全部 POST
- [x] mail-draft.controller.ts — `GET /list`, `POST /`, `DELETE /:keyId` → 全部 POST
- [x] mail-blacklist.controller.ts — `GET /list`, `POST /`, `DELETE /:keyId` → 全部 POST

## 前端 API 服务
- [x] web-user.service.ts — GET → POST
- [x] web-mail-account.service.ts — GET/PUT/DELETE → POST
- [x] web-mail-message.service.ts — GET/PUT → POST
- [x] web-mail-draft.service.ts — GET/DELETE → POST
- [x] web-mail-blacklist.service.ts — GET/DELETE → POST

## 验证
- [x] TypeScript 编译 0 错误
- [x] 端口关闭
